inventory:
  plugin: SimpleInventory
  options:
    host_file: "nornir_app/inventory/hosts.yaml"
    group_file: "nornir_app/inventory/groups.yaml"
    defaults_file: "nornir_app/inventory/defaults.yaml"

runner:
  plugin: threaded
  options:
    num_workers: 20